{
  "scripts": {
    "test": "eslint . && jest",
    "release": "webstore upload --source=ext --auto-publish"
  },
  "devDependencies": {
    "chrome-webstore-upload-cli": "^1.1.1",
    "eslint": "^4.18.2",
    "jest": "^24.8.0",
    "jest-puppeteer": "^4.1.1",
    "path": "^0.12.7",
    "puppeteer": "^1.17.0"
  }
}
